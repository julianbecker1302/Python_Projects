class Comparator:
    def __init__(self, list, search):
        self.list = list
        self.search = search
    
    def run():
        for i in range(len(self.list)):
    sys.stdout.write("\rCompared " + str(i) + " out of " + str(len(self.list)))
    sys.stdout.flush()
    if l[i] == login:
        print("\nFound!: " + self.list[i])
        break
        